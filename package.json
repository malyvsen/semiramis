{
  "name": "semiramis",
  "version": "0.0.1",
  "description": "A garden of notes.",
  "keywords": [],
  "author": "malyvsen",
  "license": "",
  "scripts": {
    "dev": "nx run-many --all --target=dev",
    "build": "nx run-many --all --target=build",
    "lint": "nx run-many --all --target=lint",
    "format": "nx run-many --all --target=format"
  },
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "nx": "^19.2.2",
    "prettier": "3.3.1"
  }
}
